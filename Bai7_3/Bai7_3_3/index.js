

//Lấy bán kính (bankinh or radius)
let radius = prompt("Nhập bán kính của hình tròn");

//Tính diện tích hình tròn (diện tích or area)
let area = Math.PI * radius * radius;
//Hiển thị kết quả
document.write("Diện tích hình tròn: " + area);
